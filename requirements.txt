attrs==26.1.0
certifi==2026.5.20
charset-normalizer==3.4.7
contourpy==1.3.2
cycler==0.12.1
cyclopts==4.16.1
docstring_parser==0.18.0
fonttools==4.63.0
idna==3.17
kiwisolver==1.5.0
markdown-it-py==4.2.0
matplotlib==3.10.9
mdurl==0.1.2
noise==1.2.2
pytest>=7.0
# Optional — install for the corresponding features:
# tetgen       -- tetrahedralisation (pip install tetgen)
# pymeshfix    -- watertight repair  (pip install pymeshfix)
# meshio       -- .msh/.inp/.vtu FEM I/O (pip install meshio)
# trimesh      -- glTF/GLB export (pip install trimesh)
# nibabel      -- GIFTI + NIfTI neuro export (pip install nibabel)
# networkx     -- graph export .graphml/.gexf (pip install networkx)
# scipy        -- fast overlay resampling + kNN graph edges (pip install scipy)
# trame        -- browser-served tuner (pip install trame trame-vuetify trame-vtk)
# noise        -- Perlin folding (pip install noise) [already in requirements above]
numpy==2.2.6
packaging==26.2
pillow==12.2.0
platformdirs==4.10.0
pooch==1.9.0
Pygments==2.20.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pyvista==0.48.4
requests==2.34.2
rich==15.0.0
rich-rst==2.0.1
scooby==0.11.2
six==1.17.0
tomli==2.4.1
typing_extensions==4.15.0
urllib3==2.7.0
vtk==9.6.2
